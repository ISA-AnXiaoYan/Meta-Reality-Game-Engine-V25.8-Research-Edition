# Research variants

<!-- SPDX-License-Identifier: AGPL-3.0-only -->

这里保存原创但未必生产启用的实验、Shadow、Dry-run、失败和撤回实现。每个变体必须包含 `STATUS.md`，说明假设、输入、输出、证据、适用条件和不可宣称项。

变体可以复用公开 Contracts/Replay，但不能直接写生产权威、读取私有运行目录或把默认主链的坐标/过滤假设静默改掉。
